[package]
name = "abc170"
version = "0.1.0"
authors = ["takahiro-itazuri <takahiro.lab.1226@gmail.com>"]
edition = "2018"

# dependencies added to new project
[dependencies]
proconio = { version = "0.3.6", features = ["derive"] }

[profile.release]
lto = true
panic = 'abort'
